{
  "name": "snow",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/snow/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project snow --web",
  "targets": {
    "generate-docs": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx ts-node scripts/generate-docs.ts"
      }
    }
  }
}